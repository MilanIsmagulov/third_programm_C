#include <stdio.h> 
#include <stdlib.h>

char byte_memory; /*Тип char позволяет хранить данные размером в один байт т.е. значения N=2^8=256 [0; 255]... byte_memory - имя переменной в языке Си принята следующая конструкция наименования переменных <тип переменной><имя переменной>; <-- знак точка с запятой не является обозначением конца строки, а является частью конструкии*/


int main(void){

    return 0;
}